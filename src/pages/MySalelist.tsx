import React from "react";
import Layout from "../components/myPage/_Layout";

const MySalelist = () => {
  return (
    <Layout title="판매목록">
      <div className="h-[100vh]">
        <h1> MySalelist </h1>
        <div>MySalelist 판매내역 중고 카드. 아이템 컴포넌트 활용</div>
      </div>
    </Layout>
  );
};

export default MySalelist;
